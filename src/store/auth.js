// import * as Constants from './constant'

export const state = {
  token: null
}

export const getters = {
  token: (state, getters) => {
    return state.token
  }
}

export const mutations = {
}

export const actions = {

}
